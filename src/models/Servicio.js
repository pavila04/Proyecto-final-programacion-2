export class Servicio {
    constructor(id, nombre, descripcion, precioBase, duracionMinutos) {
        this.id = id;
        this.nombre = nombre;
        this.descripcion = descripcion;
        this.precioBase = precioBase;
        this.duracionMinutos = duracionMinutos;
    }
}
